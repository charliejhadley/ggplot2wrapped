# add_geom_usage_to_files

`add_geom_usage_to_files()` is the main function you'll use to add geom
usage data to your files.

## Usage

``` r
add_geom_usage_to_files(
  data_file_info,
  data_geoms = ggplot2wrapped::data_geoms
)
```
