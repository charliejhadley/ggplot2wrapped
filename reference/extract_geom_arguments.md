# extract_geom_arguments

`extract_geom_arguments()` extracts and labels arguments as aes() or not
from a single geom call

## Usage

``` r
extract_geom_arguments(geom_call)
```

## Arguments

- geom_call:

  A single call of a geom, provided as a string.
