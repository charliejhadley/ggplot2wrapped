# Get geoms from a package

`get_geoms_from_package()` returns a data.frame containing all geoms
from a package.

## Usage

``` r
get_geoms_from_package(package_name)
```

## Arguments

- package_name:

  Package name, as a string

## Value

A tibble.
